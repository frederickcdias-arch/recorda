{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run migrate --workspace=@recorda/backend && npm run start --workspace=@recorda/backend",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
