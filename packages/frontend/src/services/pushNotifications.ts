/**
 * Placeholder para a fase futura de push real.
 *
 * Nesta fase o frontend mostra apenas os prompts de instalacao do PWA e
 * ativacao de notificacoes. Nao ha registro de subscription, uso de
 * PushManager nem chamada ao backend de push.
 */

export async function ensurePushSubscription(): Promise<void> {
  return;
}

export async function deactivateCurrentPushSubscription(): Promise<void> {
  return;
}
